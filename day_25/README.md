## Day 25: creating event listeners in JS.

This is my **Day 25** of javascript for cybersecurity journey. Today I focused on creating event listeners in JS.

## Overview
    -document.addEventListener allows us to add event listeners to HTML elements.
        


## What We Learned
    - I learned how to add event listeners to HTML elements using the `document.addEventListener()` method.
    - "click" event listener
        This listen when there is click in the element.