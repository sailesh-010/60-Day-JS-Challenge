## Day 22: Changing Styles in the DOM

This is my **Day 22** of javascript for cybersecurity journey. Today I focused on changing styles in the DOM using JavaScript.

## Overview
     - In this day, I focused on changing styles in the DOM using JavaScript.
     - I learned about the style property, which allows me to change the style of an HTML element.
     - I learned about the classList property, which allows me to add, remove, or toggle CSS classes of an HTML element.


## What We Learned
     - I learned how to change the style of an HTML element using the style property.
     - I learned how to add, remove, or toggle CSS classes of an HTML element using the classList property.

## Security
     - I learned that changing styles in the DOM can be a security risk if not done properly.
     - I learned that it is important to validate user input before using it to change styles in the DOM.

